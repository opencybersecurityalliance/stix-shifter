{
  "default": [
    "initiator.id as initiator_id",
    "initiator.name as initiator_name",
    "initiator.credential.type as initiator_credential_type",
    "initiator.host.address as initiator_host_address",
    "ALCH_ACCOUNT_ID as alch_account_id",
    "ALCH_TENANT_ID as alch_tenant_id",
    "eventTime as eventTime",
    "action as action",
    "target.id as target_id",
    "target.name as target_name",
    "target.host.address as target_host_address",
    "event_uuid as event_uuid",
    "observer.host.address as observer_host_addres",
    "observer.name as observer_name",
    "observer.host.address as observer_host_address",
    "api.name as api_name"
  ]
}
